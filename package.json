{
	"private": true,
	"name": "graphql-codegen-hasura",
	"version": "4.9.1",
	"description": "A collection of codgen plugins to support Hasura/Apollo GraphQL development",
	"author": "jamshally",
	"license": "MIT",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/ablestack/graphql-codegen-hasura"
	},
	"scripts": {
		"bootstrap": "lerna bootstrap",
		"build": "tsc --build --verbose",
		"rebuild": "tsc --build --force --verbose",
		"pub": "lerna publish --forcePublish",
		"lpub": "npm run build  && lerna run prepublishOnly --stream && lerna run llpub",
		"lpush": "npm run build  && lerna run prepublishOnly --stream && lerna run llpush"
	},
	"devDependencies": {
		"lerna": "^3.22.1",
		"shx": "^0.3.3",
		"ts-node": "^9.1.1",
		"typescript": "^4.1.3",
		"yalc": "^1.0.0-pre.49"
	}
}
